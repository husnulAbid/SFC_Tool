version: '3.9'

services:
  django:
    image: django-docker:0.0.1
    build: .
    environment:
      - DATABASE_NAME=sfc_tool_2
      - DATABASE_PASSWORD=qwerty1234
      - HOST_DB=sfc-tool-2.cpg4cckyiqh0.eu-north-1.rds.amazonaws.com
      - FLASK_APP_ENDPOINT=http://172.20.0.2:5100
    ports:
      - "7800:7800"
    command: >
      bash -c "python sfc_tool/manage.py migrate
      && python sfc_tool/manage.py runserver 0.0.0.0:7800"